# Java Homework Repository - Домашние задания по Java

[![Java Homework Tests](https://github.com/nicepeopleproject/first-project-2023-java/actions/workflows/homework-tests.yml/badge.svg)](https://github.com/nicepeopleproject/first-project-2023-java/actions/workflows/homework-tests.yml)

Репозиторий с домашними заданиями по Java для начинающих программистов с автоматической проверкой через CI/CD.

## Описание

Этот репозиторий содержит 5 домашних заданий различного уровня сложности, охватывающих основные концепции программирования на Java:

1. **HW1: Calculator** - Базовые арифметические операции
2. **HW2: Array Operations** - Работа с массивами
3. **HW3: String Manipulation** - Работа со строками
4. **HW4: Bank Account** - Объектно-ориентированное программирование
5. **HW5: Student Management** - Работа с коллекциями

## Требования

- **Java 17** или выше
- **Maven 3.6+**
- Git

## Установка

1. Клонируйте репозиторий:
```bash
git clone https://github.com/nicepeopleproject/first-project-2023-java.git
cd first-project-2023-java
```

2. Убедитесь, что у вас установлены Java 17 и Maven:
```bash
java -version
mvn -version
```

## Структура проекта

```
first-project-2023-java/
├── src/
│   ├── main/java/org/example/homework/
│   │   ├── hw1_calculator/
│   │   │   └── Calculator.java
│   │   ├── hw2_arrays/
│   │   │   └── ArrayOperations.java
│   │   ├── hw3_strings/
│   │   │   └── StringManipulator.java
│   │   ├── hw4_bankaccount/
│   │   │   └── BankAccount.java
│   │   └── hw5_students/
│   │       ├── Student.java
│   │       └── StudentManager.java
│   └── test/java/org/example/homework/
│       ├── hw1_calculator/
│       │   └── CalculatorTest.java
│       ├── hw2_arrays/
│       │   └── ArrayOperationsTest.java
│       ├── hw3_strings/
│       │   └── StringManipulatorTest.java
│       ├── hw4_bankaccount/
│       │   └── BankAccountTest.java
│       └── hw5_students/
│           └── StudentManagerTest.java
├── .github/workflows/
│   └── homework-tests.yml
├── pom.xml
└── README.md
```

## Домашние задания

### 📝 Homework 1: Calculator (Калькулятор)

**Файл:** `src/main/java/org/example/homework/hw1_calculator/Calculator.java`

**Задачи:**
- Реализовать сложение, вычитание, умножение, деление
- Реализовать возведение в степень и извлечение квадратного корня
- Обработать граничные случаи (деление на ноль, отрицательные числа для корня)

**Запуск тестов:**
```bash
mvn test -Dtest=CalculatorTest
```

### 📚 Homework 2: Array Operations (Операции с массивами)

**Файл:** `src/main/java/org/example/homework/hw2_arrays/ArrayOperations.java`

**Задачи:**
- Найти максимум и минимум в массиве
- Вычислить сумму и среднее значение
- Перевернуть массив
- Найти индекс элемента

**Запуск тестов:**
```bash
mvn test -Dtest=ArrayOperationsTest
```

### 🔤 Homework 3: String Manipulation (Работа со строками)

**Файл:** `src/main/java/org/example/homework/hw3_strings/StringManipulator.java`

**Задачи:**
- Проверить, является ли строка палиндромом
- Подсчитать количество гласных букв
- Перевернуть строку
- Подсчитать количество слов
- Проверить анаграммы
- Удалить пробелы

**Запуск тестов:**
```bash
mvn test -Dtest=StringManipulatorTest
```

### 🏦 Homework 4: Bank Account (Банковский счет - ООП)

**Файл:** `src/main/java/org/example/homework/hw4_bankaccount/BankAccount.java`

**Задачи:**
- Создать класс банковского счета
- Реализовать операции пополнения и снятия денег
- Реализовать перевод между счетами
- Добавить валидацию операций

**Запуск тестов:**
```bash
mvn test -Dtest=BankAccountTest
```

### 👨‍🎓 Homework 5: Student Management (Управление студентами - Коллекции)

**Файлы:**
- `src/main/java/org/example/homework/hw5_students/Student.java`
- `src/main/java/org/example/homework/hw5_students/StudentManager.java`

**Задачи:**
- Создать класс Student с валидацией
- Реализовать систему управления студентами
- Добавить операции поиска и фильтрации
- Вычислить средний балл и найти лучшего студента

**Запуск тестов:**
```bash
mvn test -Dtest=StudentManagerTest
```

## Как работать с заданиями

### Шаг 1: Выберите задание

Откройте файл с заданием, например:
```bash
src/main/java/org/example/homework/hw1_calculator/Calculator.java
```

### Шаг 2: Реализуйте методы

Найдите комментарии `// TODO: Реализуйте метод` и замените строку:
```java
throw new UnsupportedOperationException("Метод не реализован");
```

на вашу реализацию.

### Шаг 3: Запустите тесты

Запустите тесты для проверки вашего решения:

```bash
# Запустить все тесты
mvn test

# Запустить тесты конкретного задания
mvn test -Dtest=CalculatorTest
```

### Шаг 4: Проверьте результаты

После запуска тестов вы увидите:
- ✅ Зеленые тесты - задание выполнено правильно
- ❌ Красные тесты - есть ошибки, нужно исправить

### Шаг 5: Commit и Push

После успешного прохождения тестов:

```bash
git add .
git commit -m "Completed Homework 1: Calculator"
git push
```

## Автоматическая проверка (CI/CD)

Репозиторий настроен с GitHub Actions для автоматической проверки:

- ✅ Тесты запускаются автоматически при каждом `push` или `pull request`
- 📊 Результаты доступны во вкладке **Actions** на GitHub
- 🔍 Каждое домашнее задание проверяется отдельно
- 📈 Статус проверки отображается в бейдже вверху README

### Просмотр результатов CI/CD

1. Перейдите в репозиторий на GitHub
2. Откройте вкладку **Actions**
3. Выберите последний запуск workflow
4. Посмотрите результаты каждого задания

## Полезные команды Maven

```bash
# Компиляция проекта
mvn compile

# Запуск всех тестов
mvn test

# Очистка и запуск тестов
mvn clean test

# Запуск конкретного теста
mvn test -Dtest=CalculatorTest

# Создать отчет по тестам
mvn surefire-report:report

# Пропустить тесты при сборке
mvn package -DskipTests
```

## Советы для выполнения заданий

### Общие рекомендации

1. **Читайте комментарии** - в каждом методе есть подсказки
2. **Запускайте тесты часто** - это поможет быстрее найти ошибки
3. **Начинайте с простого** - выполняйте задания по порядку
4. **Изучайте тесты** - они показывают, как должен работать ваш код

### Homework 1 (Calculator)

- Используйте `Math.pow()` для возведения в степень
- Используйте `Math.sqrt()` для квадратного корня
- Проверяйте деление на ноль с помощью `if (b == 0)`

### Homework 2 (Arrays)

- Не забудьте проверить массив на `null` и пустоту
- Для реверса массива меняйте элементы от начала и конца
- Используйте цикл `for` или `for-each`

### Homework 3 (Strings)

- Метод `toLowerCase()` приводит строку к нижнему регистру
- `replaceAll("\\s+", "")` удаляет все пробелы
- `split("\\s+")` разбивает строку по пробелам

### Homework 4 (Bank Account)

- Всегда проверяйте параметры на корректность
- Используйте `IllegalArgumentException` для некорректных параметров
- Используйте `IllegalStateException` для недостаточного баланса

### Homework 5 (Collections)

- Используйте `ArrayList<Student>` для хранения студентов
- Метод `removeIf()` упрощает удаление элементов
- Stream API делает фильтрацию и поиск более выразительными

## Примеры использования Stream API

```java
// Фильтрация списка
List<Student> result = students.stream()
    .filter(s -> s.getAge() > 20)
    .collect(Collectors.toList());

// Поиск максимума
Optional<Student> top = students.stream()
    .max(Comparator.comparing(Student::getGpa));

// Вычисление среднего
double avg = students.stream()
    .mapToDouble(Student::getGpa)
    .average()
    .orElse(0.0);
```

## Отладка и тестирование

### Использование отладочной печати

```java
System.out.println("Debug: value = " + value);
```

### Запуск одного теста

```java
mvn test -Dtest=CalculatorTest#testAddPositiveNumbers
```

### Просмотр детального вывода

```bash
mvn test -X  # Debug mode
```

## Часто встречающиеся ошибки

### 1. NullPointerException
```java
// Плохо
public void method(String str) {
    str.length(); // Может выбросить NPE
}

// Хорошо
public void method(String str) {
    if (str == null) {
        throw new IllegalArgumentException("str cannot be null");
    }
    str.length();
}
```

### 2. Изменение оригинального массива

```java
// Если нужно вернуть новый массив
int[] newArray = Arrays.copyOf(original, original.length);
```

### 3. Сравнение строк

```java
// Плохо
if (str1 == str2)

// Хорошо
if (str1.equals(str2))
```

## Ресурсы для изучения

- [Oracle Java Tutorials](https://docs.oracle.com/javase/tutorial/)
- [Java Documentation](https://docs.oracle.com/en/java/javase/17/)
- [JUnit 5 User Guide](https://junit.org/junit5/docs/current/user-guide/)
- [Maven Documentation](https://maven.apache.org/guides/)

## Поддержка

Если у вас возникли вопросы:

1. Проверьте комментарии в коде - там есть подсказки
2. Изучите тесты - они показывают ожидаемое поведение
3. Посмотрите примеры в документации Java
4. Создайте Issue в репозитории с описанием проблемы

## Лицензия

Этот проект создан в образовательных целях.

## Удачи в изучении Java! 🚀

Помните: программирование - это навык, который развивается с практикой. Не бойтесь ошибаться и экспериментировать!
